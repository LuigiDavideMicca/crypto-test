import Head from "next/head";

const HomeMeta = () => {
    return (
        <Head>
            <title>Crypto Currency App</title>
            <meta name="description" content="A super simple crypto app" />
            <link rel="icon" href="/favicon.ico" />
        </Head>

    )
}

export default HomeMeta;